#--------------------------------------------
#-- Rellena estos datos antes de entregar:
#-- Nombre:  
#--------------------------------------------
	
# ---------------------------------------------
# - Funcion exp(a, n): Calcula a elevado a n 	
#----------------------------------------------
			
# --- a ^ 1 = a * 1
# --- a ^ 2 = a * a
# --- a ^ 3 = a * a * a
# --- ...
# --- Utiliza un bucle que realice iterativametne esas operaciones 
# --- tantas veces como indique el exponente

# --- Recomendación: Si usas registros estáticos es más sencillo....
# --- PERO asegúrate de NO violar el convenio

